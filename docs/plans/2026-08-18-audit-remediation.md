# Plan: Audit remediation — honest science, palette retune, publishable repo (v1.3.1 → v2.0.0)

## Summary

Fix every finding from the 2026-08-17 audit (https://claude.ai/code/artifact/d30e60cf-93f0-4dfe-914b-9e7af1e43806): make the README's science match the physics, retune the palette so the claims become true (neutrals lose blue-subpixel drive, comment/ANSI/accent legibility fixed, CVD-safe), harden the verifier so the new claims are enforced not just stated, fill the scope gaps, add a no-italic Roman variant, and turn the repo into something that packages cleanly and publishes to the Marketplace + Open VSX from a tag. Seven PRs, each babysat through bot review and merged before the next starts; palette changes ship as **2.0.0** (visible look change), hygiene ships first as **1.3.1** so the `crypticpy.alone` name can be claimed early.

## Scope

- **In**
  - Packaging hygiene: `.vscodeignore`, `.gitignore`, remove committed `alone-*.vsix`, `package.json` cleanup (`__metadata`, `configurationDefaults`, keywords, `pricing`), `$schema` in generated JSON, archive one-shot scripts, `plans/` → `docs/plans/`.
  - Verifier v2 (`scripts/verify-palette.mjs`): APCA, ΔE2000 on tight gaps, protan/deutan CVD pass, ANSI pairwise ΔE, whole-theme wavelength scan (all keys, all variants), README table rendering between markers.
  - Tests (`node:test`) + GitHub Actions CI (`build → diff --exit-code → verify → test → vsce package artifact`).
  - Palette retune via `themes/_src/` only: comment floor (Standard/Soft), neutral blue-drive reduction (foreground/operators/punctuation/comments/parameter/line numbers), ANSI L*/chroma ladder (theme + the four `terminal/` files), accent solid fills dimmed in Standard, ghost text ≠ comments, `*.defaultLibrary` real colour step, Types-vs-Functions CVD nudge.
  - Scope coverage: diff/log/preprocessor/constant/label/strikethrough/list/support.variable TextMate rules; Pylance + rust-analyzer + `event` semantic types.
  - `Alone Roman` variant (no italics) as a fourth `contributes.themes` entry.
  - README rewrite (science, APCA table, bold-weight fix, `editor.fontVariations`, melanopic + OS warm-shift advice, dead `via.placeholder.com` swatches replaced, Marketplace-first ordering), `CONTRIBUTING.md`, `CHANGELOG.md` for 1.3.1 and 2.0.0.
  - Release pipeline: `.github/workflows/release.yml` (tag → check → package → GitHub Release → `vsce publish` → `ovsx publish`), palette-strip PNG generator, screenshots (attempt local capture; fall back to a capture checklist), `docs/PUBLISHING.md` with the exact account/PAT steps the user runs.
- **Out**
  - Creating the Marketplace publisher, Azure DevOps PAT, Open VSX namespace/token, and the first `vsce publish` — user runs these from `docs/PUBLISHING.md`.
  - The red-only Apollo / Max Dark / Civil Twilight / Astronomical Dark variants from the 2026-05-22 roadmap.
  - JetBrains / Neovim ports; VS Code settings-sync profile.
  - Rewriting `themes/_snapshot/` — it stays the immutable v1.2.0 baseline; the snapshot test is gated on `PALETTE_CHANGED=1` for 2.0.0.

## Steps

1. **PR 1 — Packaging hygiene → v1.3.1.** `.vscodeignore` excludes `scripts/**`, `plans/**`, `docs/**`, `themes/_src/**`, `themes/_snapshot/**`, `*.vsix`, `package-lock.json`, `.github/**`, `tests/**`; `git rm` both `.vsix` files and drop the `!alone-*.vsix` whitelist; `package.json`: remove `__metadata` + `configurationDefaults`, add `"pricing": "Free"`, trim keywords to ~10; `build-themes.mjs` emits `"$schema": "vscode://schemas/color-theme"` first; move `retune.mjs` + `_extract-deltas.mjs` to `scripts/archive/`, `plans/` → `docs/plans/`; replace the 19 dead swatch images in README with plain hex chips (no images); CHANGELOG `[1.3.1]`; bump version. Verify: `npm run check`, `npx @vscode/vsce ls` shows ≤ 12 files. → after merge the user may `vsce publish` 1.3.1 to claim the name.
2. **PR 2 — Verifier v2 (reporting) + tests + CI.** Add to `verify-palette.mjs`: APCA Lc column, ΔE2000 next to every tight-gap warning, protan/deutan simulation table, ANSI pairwise ΔE2000 table, wavelength scan over every hex in every variant (not just 10 roles), and a `--write-readme` mode that renders the ladder / WCAG+APCA / palette / bracket / ANSI tables between `<!-- verify:<name>:start/end -->` markers in README (default mode diffs and fails on drift). New checks report as **warnings** in this PR (current palette fails several); they flip to hard-fail in PR 3. Add `tests/` (`node --test`): colour-math sanity (21:1, L*=100, APCA white/black), `build-themes` resolution edge cases (bare vs interpolated tokens, missing token, non-object variant root), build determinism (two runs byte-equal), snapshot equality gated on `PALETTE_CHANGED`. Add `.github/workflows/ci.yml` (Node 20/22): `npm ci` → `build:themes` → `git diff --exit-code themes/` → `verify` → `test` → `vsce package` uploaded as artifact. `npm test` script; Dependabot for `yaml`.
3. **PR 3 — Palette retune → v2.0.0 (all edits in `themes/_src/variants/*.yaml` + `terminal/*`).** Standard: comments `#5C544A→#6E665B`, doc-comments `→#787062`, foreground/variables `#C4B8A4→#C8B89A`, operators `#D4C8B8→#D8C8A8`, punctuation/parameter/inlay/line-number neutrals shifted the same way (B channel −10..−12 hex, L* held), line numbers `#4A4540→#56504A`, ghost text `#6B635A` non-italic + faint bg, `activityBarBadge/badge/statusBarItem.remote` fills `#D4A048→#A87C34` (button + focusBorder stay), `variable/property.defaultLibrary` `#B89860→#B08C50` (one real L* step under Types), Types-vs-Functions: `type.declaration`/`class.declaration` gain nothing — instead nudge Functions `#C08868→#C28466` if CVD ΔE<5 persists (decide from verifier output). Soft: same transforms scaled; comments `#4A433B→#625A50`. ANSI (all variants + `terminal/alone.conf|.toml|.itermcolors|windows-terminal.json`): blue→`#B09878`, brightBlue→`#C4AC8C`, cyan→`#C0A888`, brightCyan→`#D0BC9C`; magenta/green unchanged. Flip PR 2's warnings to hard-fail with explicit thresholds: APCA identifiers ≥ 60, all syntax ≥ 40 (Special/Strings get a 37 floor noted as intentional), comments ≥ 22 (Soft ≥ 17), CVD critical pairs ΔE2000 ≥ 5 **or** font-style differs, ANSI normal-8 pairwise ΔE2000 ≥ 10, tight-gap ΔE2000 ≥ 4. Update snapshot test expectation (`PALETTE_CHANGED=1` in CI env for this branch), CHANGELOG `[2.0.0]` palette section, bump version.
4. **PR 4 — Scope coverage.** `base.yaml` tokenColors additions (diff `markup.inserted/deleted/changed`, `meta.diff.header/range`; `token.info|warn|error|debug-token`; `keyword.control.directive`, `entity.name.function.preprocessor`; `variable.other.constant`, `constant.other.caps`; `entity.name.label`; `markup.strikethrough`; `markup.list`; `support.variable`) and semantic types (`selfParameter`, `clsParameter`, `magicFunction`, `builtinConstant`, `lifetime`, `attribute`, `builtinType`, `formatSpecifier`, `escapeSequence`, `boolean`, `derive`, `event`), each bound to an existing role token so every variant inherits it (parity check enforces). Eyeball `samples/` + a `.diff` and a log file.
5. **PR 5 — Alone Roman.** Introduce `${style.italic}` (and `${style.italicOnly}` where a rule is italic-only) tokens in `base.yaml`, bind to `italic` in the three existing variants and `""`/`false` in `themes/_src/variants/alone-roman.yaml` (palette = Standard); add `contributes.themes` entry, README variant blurb, CHANGELOG line. Verifier: Roman must pass the CVD check on lightness alone (no italic cue) — expect it to force the Types/Functions decision from step 3.
6. **PR 6 — Docs.** README: rewrite "The Science" (V′(λ) table, dominant-wavelength vs spectral-content distinction, honest benefit list: luminance/halation, blue-defocus, melanopic, comfort; red-only reserved for future variants), Accessibility (WCAG + APCA side by side, CVD statement now that it's verified), fix "weight 450 bold" wording, add `editor.fontVariations: true`, melanopic + Night Shift advice, Marketplace-first ordering (hero image slot → pitch → variants → science → install → settings), move contributor docs to `CONTRIBUTING.md`; all numeric tables are verifier-rendered (from PR 2). CHANGELOG 2.0.0 docs section.
7. **PR 7 — Release pipeline + assets + handoff.** `scripts/render-palette.mjs` (dependency-free PNG encoder → `images/palette.png` per variant), screenshots under `images/` (try `code --install-extension` + macOS `screencapture -l <windowid>`; if capture is unreliable, commit the checklist and leave labelled slots), `.github/workflows/release.yml` on `v*` tags (checkout → `npm ci` → `npm run check && npm test` → `vsce package` → `gh release create` with VSIX → `vsce publish --packagePath` with `VSCE_PAT` → `ovsx publish` with `OVSX_PAT`), `docs/PUBLISHING.md` (publisher creation, PAT scopes, `vsce login`, Open VSX namespace, secrets to add, first-publish smoke test, badges to paste into README once live). Final: `npm run check && npm test`, `vsce package`, install VSIX locally and switch through all four themes; hand back with the exact commands the user runs.

## Parallel execution

Not applicable — PRs 3, 4, 5 all edit `base.yaml` / every variant yaml; PR 6's tables depend on the final palette; PR 2 must land before PR 3 (warnings → hard-fails). Single agent, sequential PRs, each babysat to merge before the next branch is cut.

## Testing

- `npm run check` (build + verify) green at the end of every PR; new hard-fail thresholds green from PR 3 on.
- `npm test` (`node --test tests/`) green from PR 2 on; snapshot test passes with `PALETTE_CHANGED=1` from PR 3.
- `npx @vscode/vsce ls` after PR 1 lists only manifest/README/CHANGELOG/LICENSE/icon/themes (+ terminal/, samples/).
- `npx @vscode/vsce package` + `code --install-extension` smoke test after PR 3, PR 5, PR 7 (all variants, `samples/`, a diff, terminal, settings UI).
- CI workflow green on each PR; release workflow dry-run via `workflow_dispatch` with publish steps skipped when secrets are absent.

## Open questions

None outstanding — neutral retune (yes, 2.0.0), Roman variant (yes), publishing scope (pipeline + `docs/PUBLISHING.md`; user runs account setup and first publish) were confirmed on 2026-08-18.
